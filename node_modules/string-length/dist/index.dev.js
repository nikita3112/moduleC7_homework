'use strict';

var stripAnsi = require('strip-ansi');

var charRegex = require('char-regex');

var stringLength = function stringLength(string) {
  if (string === '') {
    return 0;
  }

  return stripAnsi(string).match(charRegex()).length;
};

module.exports = stringLength;