'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
exports["default"] = getWatermarks;

function _istanbulLibReport() {
  var data = _interopRequireDefault(require('istanbul-lib-report'));

  _istanbulLibReport = function _istanbulLibReport() {
    return data;
  };

  return data;
}

function _interopRequireDefault(obj) {
  return obj && obj.__esModule ? obj : {
    "default": obj
  };
}
/**
 * Copyright (c) Facebook, Inc. and its affiliates. All Rights Reserved.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */


function getWatermarks(config) {
  var defaultWatermarks = _istanbulLibReport()["default"].getDefaultWatermarks();

  var coverageThreshold = config.coverageThreshold;

  if (!coverageThreshold || !coverageThreshold.global) {
    return defaultWatermarks;
  }

  var keys = ['branches', 'functions', 'lines', 'statements'];
  return keys.reduce(function (watermarks, key) {
    var value = coverageThreshold.global[key];

    if (value !== undefined) {
      watermarks[key][1] = value;
    }

    return watermarks;
  }, defaultWatermarks);
}